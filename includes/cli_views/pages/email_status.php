
HINTS AND TIPS:

  The default <?=NAME?> MTA (Mail Transfer Agent) is msmtp ( http://msmtp.sourceforge.net/ )
  The email account settings for msmtp are set in the file /etc/msmtprc
  <?=NAME?> email addresses must be configured in the file <?=SETTINGS?>email.php


EMAIL CONFIGURATION:

  Currently emails are sent to <?=EMAIL_TO?> and will arrive from <?=EMAIL_FROM?>.
